server:
  port: 5000

spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:usercenter_db;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: false